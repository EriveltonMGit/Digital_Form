// src/types.ts

export interface Brand {
    id: string;      // ID único da marca
    nome: string;    // Nome da marca
    imagem: string;  // URL da imagem da marca
    descricao: string; // Descrição da marca
  }
  